[package]
name = "beta"
version = "0.1.0"

[dependencies]
# Same agreed version as alpha — rewritten against the seeded workspace key.
rand = "0.8"
# Disagrees with alpha's "1" — see alpha/Cargo.toml.
clashing = "2"
