[package]
name = "demo"
version = "0.0.1"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
# Path stub (see stubs/): the md5-libname fallback under test keys on the dep
# NAME `md-5` vs the referenced lib `md5` — registry vs path is invisible to it.
md-5 = { path = "../../stubs/md-5" }
