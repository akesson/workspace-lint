[package]
name = "demo"
version = "0.0.1"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
# Path stubs (see stubs/): the strum-derive assertion under test keys on dep
# NAMES — registry vs path is invisible to it.
strum = { path = "../../stubs/strum" }
strum_macros = { path = "../../stubs/strum_macros" }
