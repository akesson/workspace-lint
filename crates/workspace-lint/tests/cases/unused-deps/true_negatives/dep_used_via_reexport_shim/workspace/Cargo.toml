[workspace]
resolver = "2"
members = ["crates/demo"]
# Compile-only stub stays out of the member set so the lint's view of the
# workspace (cargo metadata --no-deps) is unchanged; without `exclude` cargo
# auto-adopts in-tree path deps as members.
exclude = ["stubs/web-time"]
