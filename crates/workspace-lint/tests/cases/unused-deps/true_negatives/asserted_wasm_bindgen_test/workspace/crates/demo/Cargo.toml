[package]
name = "demo"
version = "0.0.1"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
# Path stubs (see stubs/): the wasm-bindgen-test assertion under test keys on
# dep NAMES — registry vs path is invisible to it.
wasm-bindgen = { path = "../../stubs/wasm-bindgen" }

[dev-dependencies]
wasm-bindgen-test = { path = "../../stubs/wasm-bindgen-test" }
