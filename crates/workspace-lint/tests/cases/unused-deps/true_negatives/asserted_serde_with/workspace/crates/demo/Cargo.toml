[package]
name = "demo"
version = "0.0.1"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
# Path stub (see stubs/): the assertion under test keys on the dep NAME as it
# appears in the `with = "…"` string — registry vs path is invisible to it.
serde_with = { path = "../../stubs/serde_with" }
