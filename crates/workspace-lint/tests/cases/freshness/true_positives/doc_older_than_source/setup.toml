# The tracked doc (order 0) is older than the source it depends on (order 1),
# so freshness must flag it as stale.
[[mtime]]
path = "crates/demo/CLAUDE.md"
order = 0

[[mtime]]
path = "crates/demo/src/lib.rs"
order = 1
