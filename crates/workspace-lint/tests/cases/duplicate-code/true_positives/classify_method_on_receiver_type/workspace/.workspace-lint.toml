[lints]
default = "allow"
duplicate-code = "deny"

[duplicate-code]
min-lines = 4
min-tokens = 20

[engine]
# duplicate-code is a semantic lint: the classifier confirms merges against the
# rustc call graph, so the fixture workspace is extracted (build only).
configs = ["cargo build"]
