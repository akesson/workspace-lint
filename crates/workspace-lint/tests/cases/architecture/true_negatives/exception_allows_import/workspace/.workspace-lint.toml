[[architecture.rules]]
name = "apps-no-data-internals"
from = ["apps-*"]
deny = ["data-models::internal::**"]
exceptions = ["data-models::internal::PublicToken"]

[lints]
default = "allow"
architecture = "deny"
