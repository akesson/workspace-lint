[package]
name = "alpha"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
# A centralized dep and a local path dep are both allowed.
serde = { workspace = true }
bar = { path = "../bar" }
