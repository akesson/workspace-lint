//! Crate-level doc comment.
//! Spanning several lines that must NOT count as code.

/// Doc comment for `one`.
/// More documentation here.
/*
 * A block comment that also
 * spans multiple lines.
 */
pub fn one() {}
pub fn two() {}
